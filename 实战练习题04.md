以下是为您精心整理的50道Python编程练习题，覆盖基础语法、流程控制、数据结构、函数、算法等核心知识点，难度由浅入深（★表示难度）。题目参考经典编程题库及面试题优化而成，适合巩固前九章知识。

---

### **一、基础语法与运算符**  
1. **变量交换**：不借助临时变量交换 `a=10` 和 `b=20` 的值（★）  
   **提示**：使用链式赋值或元组解包。  
2. **幂运算**：计算 \(3^4\) 并输出结果（★）  
3. **取余应用**：判断数字 `123` 是否为偶数（★）  
4. **链式比较**：验证 `x=15` 是否在区间 `[10, 20)` 内（★）  
5. **运算符优先级**：计算 `2 + 3 * 4 ** 2 // 5`，解释计算步骤（★★）  
6. **浮点数截断**：将 `9.87` 转换为整数并说明结果（★）  
7. **类型转换**：将字符串 `"3.14"` 转为浮点数，计算其平方（★）  
8. **复数操作**：创建复数 `2+3j`，输出实部和虚部（★）  
9. **布尔逻辑**：判断表达式 `not (5 > 3 or 4 < 2)` 的值（★★）  
10. **位运算**：用按位与 `&` 判断数字 `7` 的最低有效位（★★）  

---

### **二、流程控制与循环**  
11. **水仙花数**：打印所有三位水仙花数（如 `153=1³+5³+3³`）（★★）  
12. **闰年判断**：输入年份，输出是否为闰年（★）  
13. **成绩分级**：输入分数，输出A(≥90)、B(60-89)、C(<60)（★）  
14. **打印图案**：用 `*` 打印倒三角形（5行）（★★）  
    ```python
    *****
    ****
    ***
    **
    *
    ```  
15. **兔子繁殖**：计算第12个月兔子的总数（斐波那契数列）（★★）  
16. **素数统计**：输出100~200之间的所有素数（★★）  
17. **完数查找**：找出1000以内所有完数（如 `6=1+2+3`）（★★★）  
18. **百鸡百钱**：100元买100只鸡（公鸡5元/只，母鸡3元/只，小鸡1元/3只），求所有组合（★★★）  
19. **猴子吃桃**：猴子每天吃一半加一个，第10天剩1个，求第一天总数（★★）  
20. **数字反转**：输入一个整数，逆序输出（如 `1234` → `4321`）（★）  

---

### **三、数据结构操作**  
21. **列表去重**：将列表 `[1,2,2,3,3,3]` 转换为集合，再转回列表（★）  
22. **字典合并**：合并字典 `{"a":1, "b":2}` 和 `{"c":3, "d":4}`（★）  
23. **元组拆包**：将元组 `(10, 20, 30)` 拆包到变量 `x,y,z`（★）  
24. **列表推导式**：生成 `[1,4,9,16,25]`（1~5的平方）（★）  
25. **矩阵对角线求和**：计算3×3矩阵 `[[1,2,3],[4,5,6],[7,8,9]]` 主对角线元素和（★★）  
26. **列表插入排序**：在有序列表 `[10,20,34,56,90]` 中插入 `50` 并保持有序（★★）  
27. **矩阵加法**：两个3×3矩阵对应位置相加（★）  
28. **单词统计**：统计字符串 `"hello world hello"` 中各单词出现次数（★★）  
29. **字符串处理**：输入字符串，分别统计字母、数字、空格和其他字符的个数（★★）  
30. **回文判断**：判断字符串 `"level"` 是否为回文（★）  

---

### **四、函数与高级特性**  
31. **最大公约数**：编写函数计算两数的最大公约数（★）  
32. **递归求阶乘**：用递归函数计算 `n!`（★★）  
33. **匿名函数**：用lambda表达式计算 \(f(x)=x^2+2x+1\)（★）  
34. **装饰器计时**：编写装饰器统计函数执行时间（★★★）  
35. **生成斐波那契数列**：用生成器输出前 `n` 项斐波那契数（★★）  
36. **闭包应用**：创建闭包函数实现计数器（★★）  
37. **可变参数求和**：用 `*args` 实现任意数量数字求和（★）  
38. **模拟switch语句**：用字典实现月份数字转英文名称（如 `3→March`）（★★）  
39. **文件读取**：读取文件 `data.txt` 并输出内容（★）  
40. **异常处理**：捕获 `int("abc")` 的 `ValueError` 并提示（★）  

---

### **五、算法与综合应用**  
41. **冒泡排序**：对列表 `[29,12,19,37,14]` 升序排序（★★）  
42. **二分查找**：在有序列表 `[5,7,11,22,27,33,39,52,58]` 中查找 `11`（★★）  
43. **选择排序**：实现选择排序算法（★★★）  
44. **猜数字游戏**：随机生成1~100的数，用户10次内猜测（★）  
45. **计算器**：实现加减乘除计算器，支持用户输入表达式（★★）  
46. **圆形面积计算**：输入半径，用公式 \(S=πr^2\) 计算面积（保留2位小数）（★）  
47. **加速度计算**：根据公式 \(a=(v-u)/t\) 计算加速度（\(u=5, v=25, t=10\)）（★）  
48. **日期计算**：输入年月日，输出该日期是当年的第几天（★★）  
49. **九宫格输出**：用循环打印对齐的九九乘法表（★）  
50. **类与对象**：定义 `Animal` 类及其子类 `Dog`，重写 `speak()` 方法（★★）  

---

**题目解析提示**：  
- 基础题注重语法细节（如第1题用 `a, b = b, a` 交换变量）；  
- 算法题需理清逻辑（如第18题百鸡百钱用嵌套循环优化范围）；  
- 综合题建议模块化拆分（如第45题计算器可先分离运算符判断）。  

> 更多题目解析可参考：http://mp.weixin.qq.com/s?__biz=MzI3MjM5MjE5OA==&mid=2247508345&idx=2&sn=0c9f89a2013d9ae95606e2865d2f019b  ｜ https://blog.csdn.net/qq_39241986/article/details/118004956